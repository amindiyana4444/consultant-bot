{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python3 -m pip install -r api/requirements.txt && python3 -m pip install -r bot/requirements.txt"
  },
  "deploy": {
    "startCommand": "python3 run_all.py",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
